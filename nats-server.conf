# NATS Server Configuration
# JetStream enabled with file storage

# Server settings
port: 4222
http_port: 8222

# Enable JetStream
jetstream {
  store_dir: "/data"
  max_memory_store: 1GB
  max_file_store: 10GB
}

# Logging
logtime: true